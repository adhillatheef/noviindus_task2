import 'package:flutter/material.dart';

class AppPalette {
  static const Color backgroundColor = Color.fromRGBO(23, 23, 23, 1);
  static const Color borderColor = Colors.white;
  static const Color errorColor = Colors.redAccent;
  static const Color red = Color.fromRGBO(199, 0, 0, 1);
  static const Color whiteColor = Colors.white;
  static const Color redGradient = Color.fromRGBO(199, 0, 0, 0.4);
  // static const Color gradient3 = Color.fromRGBO(255, 159, 124, 1);
  // static const Color greyColor = Colors.grey;
  // static const Color transparentColor = Colors.transparent;
}
